"use strict";
/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License;
 * you may not use this file except in compliance with the Elastic License.
 */
Object.defineProperty(exports, "__esModule", { value: true });
let counter = 0;
exports.createStore = (initialState, updater, onChangeCallback) => {
    let currentState = initialState;
    const getCurrentState = () => currentState;
    const commit = (type, payload, meta = { silent: false }) => {
        currentState = updater({
            ...currentState,
            primaryUpdate: {
                type,
                payload: { ...payload, uid: counter++ },
            },
        });
        if (!meta.silent) {
            onChangeCallback({ type, state: currentState }, meta);
        }
    };
    return { getCurrentState, commit };
};
